# Adapters

This EvalKit module runs locally on user-supplied or synthetic/tutorial data. It does not require an AuraOne API key and does not claim expert-authored benchmark status.

Use `evalkit --help` to see the command surface. Outputs are deterministic JSON or Markdown so private eval programs can inspect and version their own judgment layer.
